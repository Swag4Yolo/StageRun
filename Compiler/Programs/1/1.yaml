switch:
    comment: "Tofino1 32p ..."
    model: "Tofino1"
    # server_ip:   10.0.0.1
    # server_port: 9090 
    arch: tf1
    ports:
        - 50/-:
            speed: 100
            loopback: no

# Declare the other switches that we have and their respective configurations

program:
    Program: "1"
    HW: True
    Endpoints:
        ExternalPort:
            port: 50/-

