#### pipeline_pseudo_v3

- Simplifications:
    - Stage is always incrementing
    - Not considering multiple tables in each stage (this is a detail)
    - UPPER bound is the maximum number of instructions per program sequentially
